
# Setup
It's recommended to run this project on colab. To run locally, follow these steps:

- Install `uv` (https://docs.astral.sh/uv/getting-started/installation/)
- use python 3.12 `uv venv --python 3.12 --seed`
- run `source .venv/bin/activate`
- run `jupyter lab`